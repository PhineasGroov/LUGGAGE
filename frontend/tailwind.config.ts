// Tailwind CSS v4 configuration
// Configuration is now handled via @import rules in CSS
// This file is kept for compatibility but is not required for v4

export default {
  // v4 uses CSS-based configuration
  // Add custom theme values in your CSS file using @theme
}